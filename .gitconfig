[user]
	name = Dennis Herbrich
	email = dennis.herbrich+codesign@veloxis.de
	#signingkey = DEADBEEFCAFEFACE
[credential]
	helper = cache --timeout=30000
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[init]
	defaultBranch = main
[commit]
	#gpgsign = true
